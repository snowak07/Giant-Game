using UnityEngine;

public class RouteScheduler : MonoBehaviour
{
    /**
     * TODO
     * 
     * Path Point interface
     * Function that passes in position and current path (if any) and returns the nearest Path Point (and Path).
     * Create a Map.
     * Place Transforms in Paths on the map.
     */
}
